server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/Alvar0GR/Microservicios.git
          search-paths: config-data
  application:
    name: config-microservice
          
